BOR_CHAIN_URL="http://localhost:9545"
ENV="local"
MNEMONIC_DEV="poly radar mass judge dismiss just intact mind resemble fringe diary casino"

# RPC endpoints default to Tenderly's public gateways (see foundry.toml).
# No key is required for development. To use higher-rate-limit endpoints,
# override per-chain via MAINNET_RPC_URL / POLYGON_POS_RPC_URL etc.

# Scanner API keys — required by `forge script --verify`.
ETHERSCAN_API_KEY=""