# Exclude test files from duplication detection
sonar.cpd.exclusions=**/test/**/*,**/test-bor-docker/**/*

# Optional: Exclude test files from coverage requirements
# sonar.coverage.exclusions=**/test/**/*,**/test-bor-docker/**/*

# Optional: Set duplication threshold (default is 100 tokens)
# sonar.cpd.solidity.minimumtokens=100
